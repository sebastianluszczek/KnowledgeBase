extends layout

block content
    .article
        h1 #{article.title}
        h3 Author: #{article.author}
        p #{article.body}
        br
        hr
        br
        a.btn(href="/articles/edit/"+article.id) Edit
        a.btn#delete(href='#', data-id=article.id) Delete